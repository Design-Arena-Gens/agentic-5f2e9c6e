# AI Forex Trading Bot Configurator

This project provides a web UI to generate a configuration for a Python MetaTrader 5 connector that executes strategies locally on your MT5 terminal.

- Next.js app deployable to Vercel
- Client-side config generator (no server storage)
- Python connector reads base64 config and places orders via MetaTrader5
